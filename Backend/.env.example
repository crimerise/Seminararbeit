# Copy this file to .env and fill in your database connection URL
# Example for local Postgres: postgresql://user:password@localhost:5432/dbname

DATABASE_URL=
PORT=3000

# JWT settings
# Use a strong random secret in production (store in vault or environment)
JWT_SECRET=replace_this_with_a_real_secret
ACCESS_EXPIRES=15m
REFRESH_EXPIRES_DAYS=14

